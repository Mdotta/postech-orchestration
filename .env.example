# Variaveis compartilhadas entre build de imagens, Kubernetes e Docker Compose
DOCKER_USER=seu_usuario
IMAGE_TAG=local

# Endpoint para acesso via NodePort no Kubernetes
LOAD_BALANCER_IP=127.0.0.1

# Integracao de e-mail usada pela notifications-api
BREVO_API_KEY=CHANGE_TO_YOUR_BREVO_API_KEY
