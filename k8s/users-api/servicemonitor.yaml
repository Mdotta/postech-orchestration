apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: users-api
  labels:
    app: users-api
spec:
  selector:
    matchLabels:
      app: users-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
